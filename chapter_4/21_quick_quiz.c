#include <stdio.h>

int main()
{
    // int i = 0;
    // while loop me 10 se 20 tak numbers print karne hai
    // ? hint: if els

    // int i = 0;

    // while (i <= 20)
    // {
    //     if (i>=10 && i<=20)
    //     {
    //         printf("your numnber is %d\n", i);
    //     }
    //     i++;
    // }

    // ? quick quiz :
    // ? mujhe 10 se 1 tak ulte numbers print karne hai

    int v = 10;

    while (v >= 1)
    {
        printf("your number is %d\n", v);
        v--;
    }
    

    return 0;
}